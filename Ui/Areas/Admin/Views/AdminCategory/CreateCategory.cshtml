@model CreateCategoryDto
@{
    ViewData["Title"] = "CreateCategory";
    Layout = "~/Views/AdminLayout/Index.cshtml";
}

<div class="page-content">
    <div class="container-fluid">
        <div class="row">
            <div class="col-xl-12">
                <div class="card">
                    <div class="card-body">
                        <h4 class="card-title">Blog Category Page</h4>
                        <p class="card-subtitle mb-4">You can input data into the Blog Category field using the field provided below.</p>
                        <form method="post">
                            <div class="form-group">
                                <label for="exampleInputTitle">Category Name</label>
                                <input type="text" class="form-control" id="exampleInputTitle" asp-for="Name" placeholder="Add Category Name">
                            </div>
                            <button type="submit" class="btn btn-primary waves-effect waves-light">Save Category Details</button>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>